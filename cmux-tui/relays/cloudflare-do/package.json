{
  "name": "cmux-cloudflare-relay",
  "private": true,
  "scripts": {
    "build": "worker-build --release --no-panic-recovery",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.113.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  }
}
