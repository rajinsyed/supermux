[package]
name = "cmux-conformance-rust"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
cmux_client = { package = "cmux-client", path = "../../../../rust" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
